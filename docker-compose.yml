services:
  rulebox:
    build: .
    ports:
      - "8080:8080"
    environment:
      # 必须在 .env 文件中设置，或使用 `docker compose --env-file .env up` 启动
      # 未设置时服务将拒绝启动
      - VIEW_TOKEN
      - ADMIN_TOKEN
      - DATA_DIR=/data
    volumes:
      - ./data:/data
    restart: unless-stopped
